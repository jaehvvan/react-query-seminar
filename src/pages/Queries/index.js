import BasicHeaderNav from '../../components/BasicHeaderNav';

const Queries = () => {
  return (
    <section className="Queries">
      <BasicHeaderNav />
    </section>
  );
};

export default Queries;
